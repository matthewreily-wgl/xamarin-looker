<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:XamarinLooker" x:Class="XamarinLooker.Views.PerformLookView"
             xmlns:viewModelBase="clr-namespace:XamarinLooker.ViewModels.Base;assembly=XamarinLooker"
             viewModelBase:ViewModelLocator.AutoWireViewModel="true">
    <StackLayout>
        <!-- Place new controls here -->
        
        <Label Text="Perform the Look!" HorizontalOptions="Center" VerticalOptions="CenterAndExpand" />
        <Image Source="{Binding ImageSource}"/>
        <Button Text="Take Photo" Command="{Binding TakePhotoCommand}"/>
    </StackLayout>
</ContentPage>